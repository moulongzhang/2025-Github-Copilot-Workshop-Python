# ポモドーロタイマー 実装機能一覧

## 1. タイマー機能

| # | 機能 | 詳細 |
|---|---|---|
| 1-1 | カウントダウン | 25:00から0:00まで1秒ごとに減算 |
| 1-2 | 開始／一時停止 | ボタン1つでトグル。ラベルも「開始」↔「一時停止」で切り替え |
| 1-3 | リセット | 現在のフェーズの初期時間に戻す |
| 1-4 | フェーズ自動切替 | 作業(25分)終了 → 休憩(5分) → 作業(25分) と自動遷移 |
| 1-5 | フェーズ表示 | 「作業中」「休憩中」のラベル表示 |
| 1-6 | タイムアップ通知 | フェーズ終了時にブラウザ通知または音でアラート |

---

## 2. 円形プログレスバー

| # | 機能 | 詳細 |
|---|---|---|
| 2-1 | SVG描画 | 残り時間に応じた `stroke-dashoffset` の動的更新 |
| 2-2 | 色の切り替え | 作業中は紫系、休憩中は別色（グリーン等） |
| 2-3 | 時間テキスト表示 | 円の中央に `MM:SS` 形式で表示 |

---

## 3. 進捗表示（「今日の進捗」パネル）

| # | 機能 | 詳細 |
|---|---|---|
| 3-1 | 完了セッション数の表示 | 今日完了したポモドーロ数を表示（例：4） |
| 3-2 | 集中時間の表示 | 完了数 × 25分を「X時間Y分」形式で表示 |
| 3-3 | リアルタイム更新 | セッション完了のたびに即座に画面を更新 |

---

## 4. データ永続化

| # | 機能 | 詳細 |
|---|---|---|
| 4-1 | セッションの保存 | 完了時に `/api/sessions` へ POST |
| 4-2 | セッション一覧の取得 | 画面ロード時に `/api/sessions` から GET |
| 4-3 | 日付フィルタリング | 今日分のセッションのみを集計対象にする |

---

## 5. UI・UX

| # | 機能 | 詳細 |
|---|---|---|
| 5-1 | レスポンシブレイアウト | モバイル・デスクトップ両対応 |
| 5-2 | 紫系カラーテーマ | モックに合わせた配色（`#6C63FF` 系） |
| 5-3 | 角丸ボタンスタイル | 「開始」は塗りつぶし、「リセット」はアウトライン |
| 5-4 | ページタイトル更新 | タブに残り時間を表示（例：`25:00 - ポモドーロ`） |

---

## 6. Flask API

| # | 機能 | エンドポイント |
|---|---|---|
| 6-1 | メインページ配信 | `GET /` |
| 6-2 | セッション一覧取得 | `GET /api/sessions?date=YYYY-MM-DD` |
| 6-3 | セッション保存 | `POST /api/sessions` |

---

## 優先度整理

```
Must（コア機能）
  1-1 カウントダウン
  1-2 開始／一時停止
  1-3 リセット
  1-4 フェーズ自動切替
  2-1〜2-3 円形プログレスバー
  3-1〜3-3 進捗パネル
  4-1〜4-3 データ永続化
  6-1〜6-3 Flask API

Should（品質向上）
  1-5 フェーズ表示ラベル
  1-6 タイムアップ通知
  5-1〜5-4 UI/UX細部

Could（あると嬉しい）
  ページリロード後の状態復元（localStorage）
  作業時間・休憩時間のカスタマイズ設定
```
