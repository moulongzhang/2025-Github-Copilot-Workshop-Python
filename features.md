# ポモドーロタイマーアプリケーション 実装機能一覧

## 概要
このドキュメントは、ポモドーロタイマーWebアプリケーションに実装する必要のある機能を一覧化したものです。

---

## 1. コアタイマー機能

### 1.1 25分カウントダウン機能
- **1秒ごとの時間更新**
  - setInterval/requestAnimationFrameを使用した正確な時間管理
  - タイマーの状態管理（停止中/実行中/完了）

- **MM:SS形式での時間表示**
  - 25:00 → 24:59 → ... → 00:00 の形式
  - ゼロパディング対応（例: 5分 → 05:00）

### 1.2 タイマー制御
- **開始ボタン**
  - タイマーを開始する
  - 実行中は無効化（多重起動防止）

- **リセットボタン**
  - タイマーを25:00に戻す
  - 進捗状況もリセット

- **タイマー完了時の処理**
  - 自動的にセッションカウントをアップ
  - 完了通知（将来的に音声通知も検討）

---

## 2. UI/UXコンポーネント

### 2.1 円形プログレスバー
- **SVGを使用した視覚的な進捗表示**
  - 経過時間に応じて0% → 100%にアニメーション
  - グラデーションカラー（紫〜青）の適用
  - スムーズなアニメーション

### 2.2 ステータス表示
- **現在の状態を示すラベル**
  - "作業中" の表示
  - タイマーの現在状態を視覚的に示す

### 2.3 ボタンUI
- **開始ボタン**
  - 塗りつぶしスタイル（ソリッドボタン）
  - ホバー時の視覚フィードバック
  - クリック時のアニメーション

- **リセットボタン**
  - アウトラインスタイル
  - ホバー時の視覚フィードバック

---

## 3. 進捗トラッキング機能

### 3.1 今日の完了セッション数
- **完了したポモドーロの数を表示**
  - 例: "4 完了"
  - セッション完了時に自動でカウントアップ
  - 日次でリセット（翌日は0から）

### 3.2 今日の集中時間
- **合計集中時間の表示**
  - 例: "1時間40分"
  - 分単位での時間集計
  - 日次でリセット（翌日は0から）

---

## 4. バックエンドAPI（Flask）

### 4.1 セッション記録API
- **`POST /api/session/start`**
  - セッション開始時刻の記録
  - セッションIDの生成と返却
  - タイムスタンプの記録

- **`POST /api/session/complete`**
  - セッション完了時刻の記録
  - セッションデータの永続化
  - 統計情報の更新

### 4.2 統計取得API
- **`GET /api/stats/today`**
  - 今日の完了セッション数を取得
  - 今日の合計集中時間を取得
  - JSON形式でレスポンス

---

## 5. データ永続化

### 5.1 セッションデータの保存
- **各セッションの記録**
  - セッションID
  - 開始時刻
  - 完了時刻
  - セッション時間（25分固定）

### 5.2 統計情報の管理
- **日次統計の集計**
  - 日付ごとの完了セッション数
  - 日付ごとの合計集中時間

### 5.3 日次リセット機能
- **日付変更時の処理**
  - 新しい日の統計を0から開始
  - 過去データは保持

---

## 6. レスポンシブデザイン

### 6.1 ウィンドウサイズ対応
- **中央配置レイアウト**
  - タイマーUIの中央表示
  - 適切な余白とパディング

- **フォントサイズの調整**
  - 読みやすいフォントサイズ
  - デバイスに応じた調整

---

## 実装優先度（MVP）

### Phase 1: 最小限の動作（MVP）
1. ✅ **25分カウントダウン機能** - 最優先
2. ✅ **開始/リセットボタン** - 最優先
3. ✅ **時間表示（MM:SS形式）** - 最優先
4. ✅ **円形プログレスバー** - 高優先
5. ✅ **完了セッション数カウント** - 高優先
6. ✅ **集中時間の計算と表示** - 高優先

### Phase 2: データ永続化
7. バックエンドAPIの実装
8. データベース連携
9. 統計情報の保存

### Phase 3: 拡張機能（将来的）
- 休憩時間機能（5分/15分）
- カスタムタイマー設定
- 音声通知
- テーマカラー変更
- 週次/月次統計
- グラフ表示

---

## 技術スタック

### フロントエンド
- HTML5
- CSS3（グラデーション、アニメーション）
- JavaScript（Vanilla JS）
- SVG（プログレスバー）

### バックエンド
- Flask（Python）
- SQLite/PostgreSQL（データベース）
- REST API

### テスト
- pytest（バックエンド）
- Jest（フロントエンド、将来的に）

---

## 成功基準
- ✅ タイマーが正確に25分カウントダウンする
- ✅ プログレスバーが滑らかにアニメーションする
- ✅ セッション完了時に統計が正しく更新される
- ✅ リセットボタンでタイマーと進捗が初期化される
- ✅ 今日の統計が正確に表示される
