# ポモドーロタイマーWebアプリ アーキテクチャ案

## 1. ディレクトリ構成例

```
/workspaces/2025-Github-Copilot-Workshop-Python/
├── app.py                # Flaskアプリのエントリポイント
├── templates/
│   └── index.html        # メインUI（Jinja2テンプレート）
├── static/
│   ├── css/
│   │   └── style.css     # カスタムスタイル
│   └── js/
│       └── timer.js      # タイマーのロジック
└── ...（既存ファイル）
```

## 2. バックエンド（Flask）
- 役割: 静的ファイル・テンプレート配信、履歴API等の提供
- app factoryパターン（`create_app()`）でテスト容易性を確保
- ビジネスロジックはルーティングから分離し、独立したモジュール/クラスで実装
- 必要に応じてAPI（例: `/api/history`）を用意

## 3. フロントエンド（HTML/CSS/JavaScript）
- UIはJinja2テンプレート（`index.html`）で構築
- CSSでUIモックを忠実に再現
- JavaScriptでタイマー制御・状態管理・UI動的更新
  - タイマー状態・遷移・残り回数・進捗管理をクラス/関数で一元化
  - setIntervalやDate.now()等の依存はラップし、テスト時にモック可能に

## 4. テスト容易性のための工夫
- バックエンド: ルーティングとロジック分離、pytest＋Flaskテストクライアント活用
- フロントエンド: タイマー計算・状態遷移ロジックを関数/クラスで分離し、Jest等で単体テスト
- テンプレートは表示専用に徹し、ロジックはPython/JS側で完結
- 設定値や定数はconfig/定数モジュールで一元管理
- 副作用のある処理はラップしてモック可能に

## 5. 補足・ベストプラクティス
- シングルページ設計（ページ遷移不要）
- タイマー状態は基本フロントエンド管理、履歴保存等のみAPI経由
- UIモックの再現性を重視し、CSS設計は最初にしっかり行う
- ロジックとI/O・フレームワーク依存部分の分離、依存注入を徹底

---

このアーキテクチャにより、保守性・拡張性・テスト容易性の高いWebアプリ開発が可能です。